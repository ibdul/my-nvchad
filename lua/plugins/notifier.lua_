return {
  "vigoux/notifier.nvim",
  tags = {
    "notify",
    "ui",
  }
  description = [[
    This plugin provides some commands: :NotifierClear " Clear the vim.notify items :NotifierReplay " Replay all vim.notify items :NotifierReplay!
  ]],
  lazy = false,
  -- event ="VeryLazy",
  config = function()
    local notifier =  require'notifier'
    notifier.setup {
      -- You configuration here
    }
    -- vim.notify = notifier
  end
}
