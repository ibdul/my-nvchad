return {
  "vigoux/notifier.nvim",
  lazy = false,
  -- event ="VeryLazy",
  config = function()
    local notifier =  require'notifier'
    notifier.setup {
      -- You configuration here
    }
    -- vim.notify = notifier
  end
}
