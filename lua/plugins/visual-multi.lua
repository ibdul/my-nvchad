return {
  'mg979/vim-visual-multi',
  tags = {
    "ui",
    "selection",
  },
  description = "It's called vim-visual-multi in analogy with visual-block, but the plugin works mostly from normal mode.",
  event = "VeryLazy",
  opts = {},
  config = function() end,
}
