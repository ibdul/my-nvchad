return {
  'sbdchd/neoformat',
  tags = {
    "ui",
    "formatter",
  }
  description = [[
     A (Neo)vim plugin for formatting code.
  ]],
  event = "VeryLazy",
  -- config = function ()
  --   require("neoformat").setup()
  -- end,
}
